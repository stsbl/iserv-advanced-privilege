<div class="inset">
    <h1>{{ _('Advanced privilege assignment') }}</h1>
    <div class="row">
        <div class="col-md-6">
            <h2>{{ _('Assign privileges and group flags') }}</h2>
            {{ form(assign_form) }}
        </div>
        <div class="col-md-6">
            <h2>{{ _('Revoke privileges and group flags') }}</h2>
            {{ form(revoke_form) }}
        </div>
    </div>
    <h2>{{ _('Change owner') }}</h2>
    {{ form(owner_form) }}
</div>
