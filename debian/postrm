#!/bin/bash
set -e
shopt -s nullglob

if [ "$1" = "remove" ] || [ "$1" = "purge" ]
then
  rm -rf /var/cache/iserv/stsbl-iserv-advanced-privilege
  rm -f /var/lib/iserv/stsbl-iserv-advanced-privilege/app/cachedir
fi

#DEBHELPER#
